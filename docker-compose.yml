version: "3"

services:
  db:
    image: postgres:9.4
    environment:
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
      - POSTGRES_DB=odoo
    volumes:
      - "./db_data/:/var/lib/postgresql/data"
  web:
    build: .
    volumes:
      - ".:/src"
    ports:
      - "8009:8069"
    depends_on:
      - db
